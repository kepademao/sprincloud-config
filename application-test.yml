server:
  port: 3344
spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          username: kepademao
          password: 1biewenwo.
          uri:  https://github.com/kepademao/sprincloud-config.git
          search-paths:
            - springcloud-config
      label: main
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:7001/eureka
